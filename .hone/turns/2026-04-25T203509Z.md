# Hone turn 2026-04-25T203509Z
- target: `free-claude-code-setup`
- outcome: **improved**

## Summary
Tick 1 of free-claude-code-setup. Observed that the live dashboard (mac/bin/claude-nim-dashboard) tracks tokens but never surfaces what those tokens would have cost on Anthropic — i.e. the emotional payoff of the whole proxy was missing. TASK-0004 (savings card) is the High-priority ticket addressing this. Implemented the BACKEND half: Anthropic public list pricing constants per tier, a tier-mapping helper, a counterfactual-cost helper, and two new fields in the snapshot JSON (saved_today_usd, saved_all_usd). Restarted the dashboard. /api/usage now returns saved_today_usd=$7.94 across 288 requests today (9.16M input + 51K output tokens). Backend is the foundation; UI card lands in tick 2.

## Observations — before
```json
{
  "dashboard_endpoints": [
    "/",
    "/api/usage",
    "/api/stream"
  ],
  "ike_tasks_open": 5,
  "savings_field_in_api": false,
  "today_input_tokens": 1314245,
  "today_output_tokens": 5828,
  "today_requests": 30
}
```

## Observations — after
```json
{
  "dashboard_endpoints": [
    "/",
    "/api/usage",
    "/api/stream"
  ],
  "ike_tasks_open": 5,
  "lines_added_to_dashboard": 49,
  "saved_all_usd": 7.9428,
  "saved_today_usd": 7.9428,
  "savings_field_in_api": true,
  "today_input_tokens": 9164769,
  "today_output_tokens": 51644,
  "today_requests": 288
}
```

## Delta proposed
Tick 2: implement the UI half of TASK-0004 — add a card to the HTML grid showing saved_today_usd as the big number, with subtle copy 'estimated, based on Anthropic public list pricing'. Should slot between the Today-tokens card and Today-latency card. Simple DOM update in applySnapshot(); no new SSE plumbing needed since the field is already in the snapshot.
