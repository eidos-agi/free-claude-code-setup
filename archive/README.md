# archive/

Historical scripts kept for archaeology, not for re-running. All paths inside
are either stale or specific to the machine state when they were written
(typically /root/-scoped; we migrated to user-scoped in April 2026).

If you want to understand what happened during initial bring-up, read these.
Do NOT execute them on a fresh machine — use `../bootstrap.sh` instead.
